name: Test Runner

on:
  push:
    branches: [main]

jobs:
  test:
    runs-on: self-hosted

    steps:
      - name: Check runner
        run: echo "Hello from my laptop 🚀"
      - name: Pull latest changes
        run: cd ~/test &&git pull